import { infrastructureHttp } from './infrastructure-http';

describe('infrastructureHttp', () => {
  it('should work', () => {
    expect(infrastructureHttp()).toEqual('infrastructure-http');
  });
});
