{
  "name": "infrastructure-http",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/infrastructure/http/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/infrastructure/http",
        "main": "libs/infrastructure/http/src/index.ts",
        "tsConfig": "libs/infrastructure/http/tsconfig.lib.json",
        "assets": ["libs/infrastructure/http/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/infrastructure/http/jest.config.ts"
      }
    }
  },
  "tags": ["scope:infrastructure"]
}
