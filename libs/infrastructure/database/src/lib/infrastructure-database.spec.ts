import { infrastructureDatabase } from './infrastructure-database';

describe('infrastructureDatabase', () => {
  it('should work', () => {
    expect(infrastructureDatabase()).toEqual('infrastructure-database');
  });
});
