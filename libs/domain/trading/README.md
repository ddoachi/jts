# domain-trading

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-trading` to build the library.

## Running unit tests

Run `nx test domain-trading` to execute the unit tests via [Jest](https://jestjs.io).
